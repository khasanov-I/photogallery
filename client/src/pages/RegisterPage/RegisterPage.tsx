import { JSX } from "react";

interface RegisterPageProps {}

const RegisterPage = ({}: RegisterPageProps): JSX.Element => {
    return (
        <div>

        </div>
    );
};

export default RegisterPage;